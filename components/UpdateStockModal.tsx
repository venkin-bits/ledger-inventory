// components/UpdateStockModal.tsx
//
// ONE JOB: render the "adjust stock" popup/modal.
//
// Contains the quantity stepper (– / +), the type selector
// (Restock / Sale / Correction), an optional reason field,
// and the Save button. When submitted, it calls the stock
// movement API route — it does NOT write to Supabase directly.

// (Phase 2 implementation goes here)
export {}
