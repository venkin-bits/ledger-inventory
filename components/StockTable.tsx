// components/StockTable.tsx
//
// ONE JOB: render the outer shell of the inventory table
//          (headers, tab row, container).
//
// Individual rows are rendered by StockRow.tsx.
// Splitting table shell from row means you can change column headers
// without touching row rendering, and paginate/filter rows without
// restructuring the table component.

// (Phase 2 implementation goes here)
export {}
