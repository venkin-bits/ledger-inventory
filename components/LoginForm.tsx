// components/LoginForm.tsx
//
// ONE JOB: render the login form UI (email + password + submit button).
//
// This component only handles what the user SEES and types.
// The actual Supabase Auth call goes in app/api/auth/login/route.ts.
// Keeping them separate means you can redesign this form without
// touching authentication logic, and vice versa.

// (Phase 2 implementation goes here)
export {}
